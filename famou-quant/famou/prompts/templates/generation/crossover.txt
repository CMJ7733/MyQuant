You are an expert {{ language }} programmer tasked with combining the best aspects of several programs into one coherent solution.

<Task>
{{ task_description }}
</Task>

{% if strategy_guardrails %}
<StrategyGuardrails>
{{ strategy_guardrails }}
</StrategyGuardrails>
{% endif %}

{{ anchor_program_block }}

<CandidatePrograms>
{{ candidate_programs_block }}
</CandidatePrograms>

Guidelines:
1. Use the anchor program as the base structure.
2. Identify valuable techniques, patterns, or optimizations from the candidate programs.
3. Integrate only clearly complementary improvements into the anchor structure.
4. Ensure the combined program maintains correct functionality.
5. Avoid simply concatenating code or mixing unrelated ideas.

{% if diff_write_mode and has_evolve_block %}
6. All your changes MUST stay inside the EVOLVE-BLOCK. Any additional imports or helpers your implementation needs must also go inside the EVOLVE-BLOCK — do not repeat imports already present in the fixed prefix before the block.
{% else %}
{% if language == "python" %}
The code outside # EVOLVE-BLOCK-START and # EVOLVE-BLOCK-END remains unchanged.
{% endif %}
{% endif %}

IMPORTANT:
- Preserve the anchor program's strong core unless there is a clear reason to replace part of it.
- Use candidate code only when it provides a concrete improvement.
- Maintain the same inputs and outputs as the original programs.
- If `<StrategyGuardrails>` is present, treat it as binding engineering constraints.

# Format your solution as follows:

{% if diff_write_mode and has_evolve_block %}
Output ONLY the EVOLVE-BLOCK section. Your output must begin with `# EVOLVE-BLOCK-START` and end with `# EVOLVE-BLOCK-END`. Do NOT output any code outside these markers.
The code before `# EVOLVE-BLOCK-START` (the fixed prefix) is always present at runtime and executed first — its imports and definitions are directly available inside the EVOLVE-BLOCK without re-importing. Only add imports for packages not already present in the prefix.

```{{ language }}
# EVOLVE-BLOCK-START
# Your combined implementation here
# EVOLVE-BLOCK-END
```
{% else %}
Produce one complete runnable program.

```{{ language }}
# Your combined program here
```
{% endif %}

{% if language == "python" %}
```required_packages
# required_packages here
```
{% endif %}
